# dependencies (restored with `npm install`)
node_modules/

# packaged build output â€” distributed as GitHub Releases, not committed
share/
dist/
pkg/
build/

# local developer notes and build/test scripts (contain machine-specific paths)
DEV-NOTES.md
_dev/

# personal user data â€” NEVER commit (contains your logs path and quest progress)
settings.json
progress.json

# local backups / scratch
therapist_original.jpg
*.log

# design references / scratch inputs (not used by the app)
*reference*.png
*sketch*.png

# OS cruft
Thumbs.db
.DS_Store
